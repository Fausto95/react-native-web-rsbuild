{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "RNW + RSBuild + PNPM Workspace",
  "main": "src/index.ts",
  "dependencies": {},
  "devDependencies": {},

  "scripts": {
    "start:web": "pnpm run --filter  rnw-rsbuild start",
    "start:mobile": "pnpm run --filter  rn start"
  }
}
